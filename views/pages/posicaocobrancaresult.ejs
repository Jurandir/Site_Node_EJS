<!DOCTYPE html>
<html lang="pt-br">
<head>
    <%- include('../partials/head'); %>
</head>
<style>
    textarea {
        background-color: black;
        color: yellow;
    }
</style>
<body class="container">
  <%- include('../partials/msg'); %>

    <header>
        <%- include('../partials/header'); %>
    </header>
    <!-- posicaocobrancaresult -->

    <main>
        <div class="jumbotron">
            <h1>Posição de Cobrança</h1>
            <h5><%= empresa %></h5>

            <table class="table table-sm table-hover table-dark">
                <thead>
                  <tr>
                    <th scope="col">Código</th>
                    <th scope="col">Dt Fat.</th>
                    <th scope="col">Dt Vencto.</th>
                    <th scope="col">Dt Pagto.</th>
                    <th scope="col">Valor</th>
                    <th scope="col">Cód. Agente</th>
                    <th scope="col">Boleto</th>
                  </tr>
                </thead>
                <tbody>
                  <% dados.forEach(function(i){ %>             
                      <tr>
                      <th class="th-sm" scope="row"><a href='/posicaocobranca/fatura?&dados=<%= JSON.stringify(i)%>'><%= i.codigo%></a></th>
                      <td class="td-sm"><%= i.datafat.substring(8,10)+'-'+i.datafat.substring(5,7)+'-'+i.datafat.substring(0,4) %></td>
                      <td class="td-sm"><%= i.datavenc.substring(8,10)+'-'+i.datavenc.substring(5,7)+'-'+i.datavenc.substring(0,4) %></td>
                      <% if (i.datapag) { %>
                        <td class="td-sm"><%= i.datapag.substring(8,10)+'-'+i.datapag.substring(5,7)+'-'+i.datapag.substring(0,4) %></td>
                      <% } else { %>
                        <td class="td-sm">-</td>
                      <% } %>' 
                      <td class="td-sm"><%= i.valor%></td>
                      <td class="td-sm"><%= i.agt_codigo%></td>
                      <td class="td-sm"><%= i.bloquete%></td>
                    </tr>
                  <% })%>
                </tbody>
              </table>
              <div class="row">
                      <div class="col-3">
                          <a href="/posicaocobranca" class="btn btn-large btn-block btn-primary">Nova Pesquisa</a>
                      </div>
              </div>
    </main>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>
</html>